<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ComCrashTestApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ComCrashTestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ComCrashTestApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid ColumnSpacing="20" RowSpacing="20" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="TEST" Click="Button_Click" VerticalAlignment="Center" HorizontalAlignment="Stretch" />

        <local:MyCanvas x:Name="myCanvas" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

    </Grid>
</Window>
